<div class="nav-scrollable">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/" Match="NavLinkMatch.All">
                <span class="nav-icon">🏠</span>
                @if (IsExpanded)
                {
                    <span class="nav-label">Home</span>
                }
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/storygeneration">
                <span class="nav-icon">💡</span>
                @if (IsExpanded)
                {
                    <span class="nav-label">Generate</span>
                }
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/draft-stories">
                <span class="nav-icon">📝</span>
                @if (IsExpanded)
                {
                    <span class="nav-label">Draft Stories</span>
                }
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/scrum-board">
                <span class="nav-icon">📋</span>
                @if (IsExpanded)
                {
                    <span class="nav-label">Scrum Board</span>
                }
            </NavLink>
        </div>
    </nav>
</div>

@code {
    [Parameter]
    public bool IsExpanded { get; set; }
}