<div class="top-row ps-3 navbar navbar-dark">
    <div class="nav-fluid">
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/storygeneration">
                <span class="bi bi-lightbulb" aria-hidden="true"></span> Generate
            </NavLink>
            <NavLink class="nav-link" href="draft-stories">
                <span class="bi bi-list-task" aria-hidden="true"></span> Draft Stories
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="scrum-board">
                <span class="bi bi-kanban" aria-hidden="true"></span> Scrum Board
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
